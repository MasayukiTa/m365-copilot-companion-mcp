# セキュリティ

このツールのアクセスモデルの概要です。詳細な認証・認可の仕組みは [docs/SECURITY.md](docs/SECURITY.md) を参照してください。

---

## アクセスモデル

各インストールは次の 3 層で保護されます。

1. **インストール毎のランダム API キー** — セットアップ時に生成される `MCP_API_KEY`（40 桁のランダム hex）がないと、サーバーへのリクエストは全て 401 で弾かれます。
2. **Copilot Studio のエージェント/コネクタは既定で作成者プライベート** — 自分で作ったエージェントは、明示的に共有しない限り他の人からは呼び出せません（README STEP 5 の「公開」で「利用者を自分だけ」に設定するのはこのためです）。
3. **devtunnel の匿名公開は既定 OFF** — `MCP_TUNNEL_ALLOW_ANONYMOUS=1` を明示的に設定した場合のみ、トンネルが匿名アクセス可能になります。既定のままなら、この公開リポジトリを誰が clone してビルドしても、繋がる先は**その人自身が立てたサーバー**だけです。

---

## 共有するときの注意（重要）

Copilot Studio のエージェント/コネクタを他の人と共有すると、共有された相手は、その接続に保存されている**作成者本人の API キーに相乗りして**ツールを実行できます。これは Power Platform の implicit connection sharing（接続の暗黙共有）の一般的な性質であり、このツール固有の欠陥ではありません。

つまり、共有した時点で「誰が呼んだか」を API キー認証だけでは区別できません。**共有は、この意味を理解した上で行ってください。** 現状、利用者個人の Entra ID を API キー認証のバックエンドで検証する手段は提供していません。

---

## 組織で展開する場合

複数人で使い、かつ「誰がその操作をしたか」を利用者ごとに本人認証したい場合、現状唯一の経路は **OAuth 2.0 (Microsoft Entra ID) 構成**です。これには Entra アプリ登録（IT 管理者による 1 回限りの作業）が必要になります。組織展開を希望する場合は、管理者にアプリ登録を依頼してください。

個人利用・少人数での非公開利用（共有しない、または信頼できる相手にのみ共有）であれば、上記の API キー認証で十分です。

---

## ローカルジョブ実行の承認ゲート

タスクルーター（`relay/task_router.py`）経由でローカル実行されるジョブは、`TASK_JOB_APPROVAL_MODE` で挙動を切り替えられます。FleetCockpit の承認センターまたは承認通知からも変更でき、長時間実行中のルーターへ即時反映されます。

| 値 | 挙動 |
|---|---|
| `default`（既定） | 未知のジョブクラスは初回に承認が必要（デスクトップ通知 + Approve/Deny）。承認後は同クラスを自動実行しますが、破壊的パターンを含むペイロードは承認済みクラスでも毎回確認に戻ります |
| `auto` | 静的リスク検査のみで自動判定 — 危険パターンは拒否、要注意パターンは承認要求、問題なければ実行 |
| `bypass` | ゲート自体を無効化（従来動作） |

`bypass` でも自律契約の `stop_when`、ファイル範囲のハード制限、外部Skillの初回・内容変更時の完全ハッシュ承認は解除されません。外部Skillは1ファイル2 MiB、全体5 MiB、256ファイルを既定の取込み上限とし、承認画面に実サイズと上限を表示します。

ファイル操作は、ペイロードで明示的に `allow_outside=true` を指定しない限り、リポジトリ配下（または `TASK_FILE_ALLOWED_BASE`）に制限されます。これはモードに関わらず常時有効なハード制限です。

---

## 脆弱性の報告

脆弱性を見つけた場合は、GitHub の [Private vulnerability reporting](https://github.com/MasayukiTa/m365-copilot-companion-mcp/security/advisories/new)（このリポジトリの Security タブから "Report a vulnerability"）で報告してください。使えない場合は Issue で構いませんが、機微な内容を含む場合は Private reporting を優先してください。
