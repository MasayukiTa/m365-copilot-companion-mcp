fastmcp>=3.4.5,<4.0
# httpx + anyio are imported DIRECTLY by our code (tools/web_ops.py, tools/diagram_ops.py,
# tools/registry.py). They are also transitive deps of fastmcp/mcp today, but we declare
# them explicitly so a future framework change that drops them cannot break a fresh install.
httpx>=0.28.1
anyio>=4.14.2
pandas
openpyxl
python-pptx>=1.0.2,<2.0
python-docx>=1.2.0
pypdf>=6.14.2
send2trash>=2.1.0
matplotlib
pytesseract>=0.3.13
ddgs>=9.14.4
watchdog>=6.0.0
pyodbc>=5.3.0
psutil>=7.2.2
python-dotenv
PyYAML>=6.0.3
pytest>=9.1.1
# Browser automation for the bridge + SWE fleet. Used via connect_over_cdp (attaches to the
# already-running Edge), so NO `playwright install` browser download is needed -- the pip package
# alone is enough. (Was missing here, which broke a fresh install of the bridge/fleet.)
playwright>=1.61.0
pillow>=12.3.0
# Office / COM + OCR helper tools (Windows). Optional: only the outlook_*/pptx_export/ocr_pdf tools
# need these; pdf2image also needs the Poppler binary on PATH.
pywin32>=312; sys_platform == "win32"
pdf2image>=1.17.0
