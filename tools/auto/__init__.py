"""Generated-tool package.

Modules under this package are written by the tool foundry (tools/foundry.py).
Each forged module is importable immediately, but Copilot/MCP clients only see
its tools after the server is restarted. Keep this package importable even when
it is empty.
"""
